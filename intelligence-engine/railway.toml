# Railway config for the AGIB intelligence gather worker.
# Set the service root directory to intelligence-engine in the Railway dashboard.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python scripts/gather_worker.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
